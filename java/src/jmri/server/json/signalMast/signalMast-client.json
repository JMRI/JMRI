{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "jmri-json-signalMast-client-message",
    "type": "object",
    "description": "Schema data object in message from client to JMRI for type \"signalMast\"",
    "properties": {
        "name": {
            "type": "string",
            "description": "System name of the signalMast"
        },
        "userName": {
            "type": "string",
            "description": "If present, set the user name of the signalMast"
        },
        "comment": {
            "type": "string",
            "description": "If present, set the comment for the signalMast"
        },
        "state": {
            "type": "string",
            "description": "If present, set the state for the signalMast"
        }
    },
    "additionalProperties": false,
    "required": ["name"]
}
