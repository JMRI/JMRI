{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "jmri-json-layoutBlock-client-message",
    "type": "object",
    "description": "Schema data object in message from client to JMRI for type \"layoutBlock\"",
    "properties": {
        "name": {
            "type": "string",
            "description": "System name of the layoutBlock"
        },
        "userName": {
            "type": "string",
            "description": "If present, set the user name of the layoutBlock"
        },
        "comment": {
            "type": "string",
            "description": "If present, set the comment for the layoutBlock"
        },
        "value": {
            "type": "string",
            "description": "If present, sets the value of the layoutBlock"
        }
    },
    "additionalProperties": false,
    "required": ["name"]
}
