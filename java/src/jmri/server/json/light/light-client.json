{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "jmri-json-light-client-message",
    "type": "object",
    "description": "Schema data object in message from client to JMRI for type \"light\"",
    "properties": {
        "name": {
            "type": "string",
            "description": "System name of the light"
        },
        "userName": {
            "type": "string",
            "description": "If present, set the user name of the light"
        },
        "comment": {
            "type": "string",
            "description": "If present, set the comment for the light"
        },
        "state": {
            "type": "integer",
            "description": "If present, sets the state of the light",
            "enum": [0, 2, 4]
        }
    },
    "additionalProperties": false,
    "required": ["name"]
}
