{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "jmri-json-hello-client-message",
    "type": "object",
    "description": "Schema data object in message from client to JMRI for type \"hello\"",
    "properties": {
        "locale": {
            "type": "string",
            "description": "The client locale matching a Java Locale string representation"
        },
        "client": {
            "type": "string",
            "description": "A token the client can use to subscribe to a message stream"
        }
    },
    "additionalProperties": false,
    "required": ["name"]
}
