{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "jmri-json-audio-client-message",
    "type": "object",
    "description": "Schema data object in message from client to JMRI for type \"audio\"",
    "properties": {
        "name": {
            "type": "string",
            "description": "System name of the audio"
        },
        "userName": {
            "type": "string",
            "description": "If present, set the user name of the audio"
        },
        "comment": {
            "type": "string",
            "description": "If present, set the comment for the audio"
        },
        "state": {
            "type": "integer",
            "description": "If present, sets the state of the audio",
            "enum": [0, 2, 4]
        }
    },
    "additionalProperties": false,
    "required": ["name"]
}
