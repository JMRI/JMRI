# addendum to ecj.warnings.options for checking the tests source code
#
# We relax certain requirements for the test code; this handles that

-warn:-dep-ann
-warn:-deprecation
-warn:-allDeprecation

-warn:-allOver-ann
-warn:-over-ann

  # We allocate objects to test that code can run OK
-warn:-unusedAllocation
