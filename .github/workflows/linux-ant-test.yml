# This workflow will build a Java project with Ant
# For more information see: https://docs.github.com/en/actions/guides/building-and-testing-java-with-ant

name: Linux CI Tests with Ant

on: [ push, pull_request ]
#  push:
#    branches: [ master ]
#  pull_request:
#    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: 11
        distribution: 'adopt'
#    - name: Change screen resolution
#      run: Set-DisplayResolution -Width 1600 -Height 1200 -Force
#      shell: pwsh
#    - name: Cache Maven packages
#      uses: actions/cache@v1
#      with:
#        path: ~/.m2
#        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
#        restore-keys: ${{ runner.os }}-m2
    - name: Install ant-optional
      run: apt-get install ant-optional
    - name: Test with Ant
      run: |
        # run all tests using ant
        ant -noinput -buildfile build.xml realclean tests javadoc alltest ci-test
#        ant -noinput -buildfile build.xml ci-test
#    - name: Upload generated screenshots artifact
#      uses: actions/upload-artifact@v2
#      if: ${{ always() }}
#      with:
#        name: screenshots
#        path: temp/temp/*.png
