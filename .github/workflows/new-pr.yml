name: New PR

permissions:
  issues: write
  contents: write
  pull-requests: write

on:
  pull_request:
    types: [opened, reopened, labeled, unlabeled, synchronize]

jobs:
  check-pr:
    runs-on: ubuntu-latest

    env:
      PR_LABEL: 'New PR'
      BLOCK_HOURS: '0.25' # e.g. 0.25 for testing
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Evaluate PR age and manage label
        run: |
          # Fetch PR info
          pr_number="${{ github.event.pull_request.number }}"
          created_at="$(gh pr view $pr_number --json createdAt --jq '.createdAt')"
          label_name="${PR_LABEL}"

          # Compute PR age in hours
          now=$(date -u +%s)
          created=$(date -u -d "$created_at" +%s)
          age_hours=$(echo "scale=2; ($now - $created)/3600" | bc)
          echo "ℹ️ PR #$pr_number is ${age_hours}h old."

          # Get current labels
          labels="$(gh pr view $pr_number --json labels --jq '.labels[].name')"

          # Check and update label state
          if (( $(echo "$age_hours <= $BLOCK_HOURS" | bc -l) )); then
            if ! grep -q "$label_name" <<< "$labels"; then
              echo "🔖 Adding label '$label_name'."
              gh issue edit "$pr_number" --add-label "$label_name"
              fi
            fi

          if (( $(echo "$age_hours > $BLOCK_HOURS" | bc -l) )); then
            if grep -q "$label_name" <<< "$labels"; then
              echo "🔖 Removing label '$label_name'."
              gh issue edit "$pr_number" --remove-label "$label_name"
              fi
            fi

          # Block merging if label exists
          if gh pr view $pr_number --json labels --jq '.labels[].name' | grep -qx "$label_name"; then
            echo "❌ This PR is labeled '$label_name' and cannot be merged until ${BLOCK_HOURS}h after creation."
            exit 1
          else
            echo "✅ PR is mergeable."
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
